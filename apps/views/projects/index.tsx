import React from 'react';
import { View } from 'react-native';
import ProjectListWithHeader from './ProjectList';

const ProjectListManagement = () => {
    return (
        <View style={{flex: 1, backgroundColor:'white'
        }}>

        <ProjectListWithHeader headerName="E-CONNECT NAFFCO"/>
        </View>
    );
};

export default ProjectListManagement;
